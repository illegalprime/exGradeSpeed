<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".SettingsActivity" >

    <TextView
        android:id="@+id/courseDescription"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:text="Enter the Log In URL of your district:"
        android:textAppearance="?android:attr/textAppearanceLarge" 
        android:paddingBottom="5dip" />

    <EditText
        android:id="@+id/urlInput"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/courseDescription"
        android:layout_alignRight="@+id/courseDescription"
        android:layout_below="@+id/courseDescription"
        android:ems="10" >

        <requestFocus />
    </EditText>

    <Button
        android:id="@+id/settingsSaveBtn"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_alignRight="@+id/urlInput"
        android:layout_below="@+id/urlInput"
        android:layout_marginTop="14dp"
        android:text="Save" />

</RelativeLayout>
